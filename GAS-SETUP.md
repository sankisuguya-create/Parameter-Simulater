# 校務PC・児童端末向けGAS公開手順

## 用意するファイル

- `gas/Code.gs`
- `gas/Index.html`

`Index.html`にはCSSとJavaScriptを埋め込んであります。外部ライブラリ、GitHub、データベースへの接続はありません。

## 初回公開

1. 教師用Google Workspaceアカウントで [Google Apps Script](https://script.google.com/) を開きます。
2. 「新しいプロジェクト」を作り、名前を「ケイドロ・パラメータ実験室」にします。
3. 最初からある`コード.gs`の内容を削除し、`gas/Code.gs`の内容を貼り付けます。
4. 左側の「＋」から「HTML」を選び、ファイル名を`Index`にします。
5. `gas/Index.html`の内容をすべて貼り付け、保存します。
6. 右上の「デプロイ」→「新しいデプロイ」を開きます。
7. 種類に「ウェブアプリ」を選び、次を設定します。
   - 次のユーザーとして実行：自分
   - アクセスできるユーザー：組織内の全員
8. 「デプロイ」を押し、末尾が`/exec`のURLをコピーします。
9. 教師アカウントをログアウトしたブラウザと、児童用アカウントの端末でURLを開いて確認します。

「組織内の全員」で児童が開けない場合、教師用と児童用のドメインが管理上別組織になっている可能性があります。管理者が許可している場合だけ「Googleアカウントを持つ全員」または「全員」へ変更します。児童名や成績は保存しない設計です。

## 児童への配布

確認済みの`/exec` URLを集約サイトに一つだけ掲載します。編集者専用の`/dev` URLは配布しません。児童は同じURLを開き、「くらべる」を押して利用します。

## 更新

1. PC側で画面やシミュレーションを修正します。
2. `npm run build:gas`を実行して`gas/Index.html`を再生成します。
3. GASの`Index`を生成後の内容に置き換えて保存します。
4. 「デプロイ」→「デプロイを管理」→鉛筆アイコンを開きます。
5. バージョンを「新バージョン」にしてデプロイします。

同じ`/exec` URLのまま更新されます。古い表示が残る端末では、`Ctrl + Shift + R`で再読み込みします。

## 公開前の確認

- A・B双方の条件メニューが開く
- プリセットを選ぶと餅が0人になる
- 鬼・逃げの「走力を変える割合」と速さを変更できる
- 「くらべる」で両画面のシンボルが動く
- 捕獲時と救出時に音が鳴る
- 児童用アカウントで認証警告なしに開ける
